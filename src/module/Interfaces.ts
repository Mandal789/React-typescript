export interface LoginUser{
    username: string;
    phone: number | string;
    email: string;
  }

  export interface ApiData {
    userId: number;
    id: number;
    title: string;
    body: string;
  }